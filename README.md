# Spotify Telegram Bot

Это Telegram-бот для взаимодействия со Spotify. Позволяет авторизоваться через Spotify, искать треки, лайкать их, воспроизводить и добавлять в очередь на воспроизведение прямо из Telegram.

---

## Возможности

- Авторизация через Spotify OAuth
- Поиск треков по названию и исполнителю
- Просмотр информации о треке
- Воспроизведение трека на активном устройстве пользователя
- Добавление треков в избранное (Liked Songs)
- Добавление треков в очередь воспроизведения
- Удобная навигация между результатами поиска и информацией о треке

---

## Требования

- Python 3.9+
- PostgreSQL или другая поддерживаемая база данных (если используется ORM)
- Telegram Bot Token
- Spotify Developer Account с созданным приложением (Client ID, Client Secret)

---

## Установка и настройка

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/yourusername/spotify-telegram-bot.git
   cd spotify-telegram-bot
    ``

2. Создайте и активируйте виртуальное окружение:

   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux / macOS
   venv\Scripts\activate     # Windows
   ```

3. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

4. Создайте файл `.env` в корне проекта с содержимым:

   ```env
   BOT_TOKEN=ваш_токен_телеграм_бота
   SPOTIFY_CLIENT_ID=ваш_spotify_client_id
   SPOTIFY_CLIENT_SECRET=ваш_spotify_client_secret
   SPOTIFY_REDIRECT_URI=http://yourdomain.com:8888/callback
   SOCKS5_PROXY=socks5://user:password@host:port  # опционально
   ```

   > **Важно:** Redirect URI должен совпадать с тем, что вы указали в настройках Spotify Developer Dashboard.

5. Настройте базу данных (если используется ORM):

   * Проверьте файл `bot/database/db.py` и укажите параметры подключения.
   * Выполните миграции, если они есть.

---

## Запуск

Запустите бота и сервер для обработки Spotify OAuth callback одновременно:

```bash
python -m bot.run_bot_and_server
```

Или напрямую:

```bash
python run.py
```

После запуска бот будет доступен в Telegram.

---

## Использование

* `/start` — начать работу и получить ссылку для авторизации в Spotify.
* `/help` — информация о командах и функционале.
* `/search <запрос>` — поиск треков по названию или исполнителю.
* В результатах поиска выбирайте трек, чтобы:

  * Воспроизвести его на вашем устройстве.
  * Добавить в избранное.
  * Добавить в очередь воспроизведения.
* Кнопка «Назад к результатам поиска» позволяет вернуться к списку треков без создания нового сообщения.

---

## Структура проекта

* `bot/` — основной код бота

  * `handlers/` — обработчики команд и callback
  * `services/` — функции взаимодействия со Spotify API
  * `database/` — модели и инициализация БД
  * `config.py` — загрузка конфигурации из `.env`
  * `spotify_redirect_server.py` — aiohttp сервер для обработки OAuth callback
* `run.py` — запуск бота и aiohttp сервера
* `.env` — переменные окружения с конфиденциальными данными

---

## Логирование

* Логи выводятся в консоль и одновременно пишутся в файл `bot.log`.
* Логируются важные события: старт/стоп бота, ошибки, авторизация пользователей.

---

## Лицензия

Проект предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск.

---

## Контакты и поддержка

Если у вас есть вопросы или предложения, создавайте issue в репозитории или пишите на любой удобный контакт, указанный в профиле.

---

Спасибо за использование бота! 🎧🚀